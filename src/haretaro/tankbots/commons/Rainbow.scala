package haretaro.tankbots.commons

import robocode._
import java.awt.color

/**
 * @author Haretaro
 */
case class Rainbow(r:Robot) {
  var spector = 0
  
  def update = {
   def color(r:Int) = {
     
   } 
  }
}